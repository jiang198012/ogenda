# Changelog

## 1.3.4 (2026-08-31)

- **提醒支持多次独立编辑**：提醒改为逐行输入，点击「添加提醒」可增加多个带单位提醒，每条可单独删除；保留旧的逗号分隔和纯数字分钟兼容，并继续为每条提醒生成独立 `VALARM`。
- **修复清单视图窄屏布局**：窄面板下日期/时间上下排列，避免挤压事件标题。

## 1.3.3 (2026-08-25)

- **新增 4 条命令,日程一键带走**:「复制今日日程为文本」「复制本周日程为文本」把今天 / 本周(周一至周日)的日程格式化为纯文本放进剪贴板;「插入今日日程到当前笔记」「插入本周日程到当前笔记」以 markdown 列表插入当前光标处。与面板共用同一条事件管线,导出即所见:全天事件标「全天」排在当日最前,跨天事件按天切分(首日 `15:00-24:00`、中间整天、末日 `00:00-10:00`),重复事件逐实例展开,地点附 `@地点`,没有日程的天自动略过。

## 1.3.2 (2026-08-19)

- **修复:日程在面板上重复显示两份**。当日程目录(`Agenda/`)里存在备份快照(如 `2026-08.md.bak`)、同步残留或杂散笔记时,旧版会把其中形似事件块的内容也当成正式日程解析渲染,日视图上表现为每个日程并排出现两次。现在只认严格命名的月度文件 `YYYY-MM.md`,备份与杂散文件一律不参与解析和删改,备份本身原样保留、绝不被改写。

## 1.3.1 (2026-08-18)

- **周视图新增顶部贯通横条区**:全天事件与跨天事件(出差、团建、跨午夜班次等)不再把每一天的时间列填满,而是合并为一条横跨对应天列的贯通横条,集中在周视图顶部展示;重叠横条自动分层,越过周界的横条端头切角示意延续;时间格区域只保留单日事件,其他日程不再被遮挡,时间窗口也不再被跨天事件撑大。

## 1.3.0 (2026-08-14)

- **视图跟随分区窗口,按情况自适应**:日/周视图默认只展示设置了分区的时段(默认 06:00–23:00),清晨之前 / 深夜不占空间;但**深夜有事件时时间轴自动延伸**把事件显示出来(窗口 = 分区范围 ∪ 事件范围,只扩不缩),无事件时缩回分区窗口。
- **周视图时间刻度**:新增省略版贯通时间线——06:00 / 12:00 / 18:00 三条线从左到右横贯整周(数字标注在线下方 1 小时处),列内整点小时线同步保留;所有列共用统一窗口,线与分区色块/事件严格对齐。
- **事件标签紧凑化**:月视图 mini chip、全天横条、日/周时间格块统一为只显示标题的紧凑形式(节约空间,时间由时间格位置表达);周视图块标题字号与月视图一致。
- **新色卡**:默认六段分区换用相邻色相 ≥44° 的配色(青/蓝/黄/玫红/橙/紫),透明叠加后仍一眼可辨。
- 修复:窗口化后事件块与分区色带的位置错位(坐标基准统一);全天横条移出时间格区,贯通线不再压文字、与色块精确对齐;默认分区名称随界面语言生成(中文用户直接看到中文段名)。

## 1.2.0 (2026-08-14)

- **时间线分区**:设置里新增「时间线分区」——可增删时间段,每段可设名称、起止时间(24 小时制,支持跨午夜)与填充色。日/周视图的时间格按分区铺上半透明色带(默认 14% 透明度),一眼看清事件落在哪个时间区间;分区关闭或清空即回到无分区样式。默认开箱即用六段:清晨 06:00–08:30 / 上午 08:30–12:00 / 中午 12:00–14:00 / 下午 14:00–17:00 / 傍晚 17:00–20:00 / 晚上 20:00–23:00。
- **视图跟随分区范围**:日/周视图只展示设置了分区的时段(默认 06:00–23:00,17 小时),清晨之前 / 深夜没有配置的时段不占空间;窗口外的事件不显示,跨窗口边界的事件按窗口裁剪。无分区时回退全天 24 小时。
- **周视图时间格化**:周视图从卡片流升级为 7 列 × 24 小时纵向时间格(每小时 28px):事件按真实时间定位、重叠自动分列、全天事件走顶部横条、空白点击/拖拽划范围/拖卡片移动/拖底部改时长与日视图一致;分区色带、跨列拖拽(HTML5 DnD)都保留。
- 内部:日/周视图的时间格渲染与拖拽逻辑合并为共享模块(`time-grid.ts`),行为完全一致。

## 1.1.0 (2026-08-14)

四个新能力:

- **提醒通知**:事件可设提醒(不提醒 / 开始时 / 提前 5–30 分钟 / 1 小时 / 1 天),写入 CalDAV 的 `VALARM` 与服务器互通;设置里开启「启用事件提醒」后,Obsidian 打开期间每 30 秒检查一次,到点弹通知(重复事件按下一个实例提醒)。新事件可设默认提醒。
- **重复事件编辑**:表单新增「重复」下拉(不重复 / 每天 / 每个工作日 / 每周 / 每月 / 每年 / 自定义 RRULE,自定义规则实时校验)。点击重复事件的某个实例时可选「仅本次 / 全部 / 跳过本次」:仅本次会生成一个独立事件并在原序列上写 `EXDATE`;跳过本次只加 `EXDATE`。EXDATE 随 CalDAV 双向同步(生成、解析、落盘、比较全覆盖)。
- **日视图时间格 + 拖拽**:日视图从卡片列表升级为 00:00–24:00 时间格(小时刻度、当前时刻红线、全天事件横条、重叠事件自动分列)。鼠标操作:空白格点击 → 在该时刻新建;拖拽空白格 → 划范围新建;拖事件卡片 → 移动;拖卡片底部 → 调整结束时间。周视图 / 月视图支持把事件卡片拖到另一天(全天事件按天平移)。
- **自然语言快速创建**:命令「快速新建事件」,输入 `明天下午3点 和经理开会`、`周五 10:00 周会 1小时`、`tomorrow 3pm standup 30min` 等,自动解析日期 / 时间 / 时长 / 标题并预填事件表单(解析失败也会带标题打开表单)。

同步与存储:提醒与 EXDATE 参与本地改动检测(`base_hash`),旧事件未设置时哈希与旧版本逐字节一致,升级不会触发全量重推。

## 1.0.3 (2026-08-09)

- **月视图高密度修复**:单日超过 6 条事件时折叠为「还有 N 个」,点击展开(展开后解除裁剪,事件全部可见);限定每格高度,整月最后一周不再被撑出滚动区,始终可滚到底。
- **周视图窄屏修复**:`@container` 窄面板规则改为 `minmax(0,1fr)`,与月视图一致,480px 侧边栏下周历 7 列完整显示,不再裁掉周末列。
- **i18n 修复**:日视图详情卡「回复状态」显示本地化文案(已接受/Accepted),不再显示原始 iCal 枚举;默认分类复用 i18n key。
- **测试补充**:空月/空周/空日渲染、密集上限与展开、跨午夜(22:00→次日)、多天全天事件在周视图的跨格显示、RSVP 本地化。

## 1.0.2 (2026-08-09)

- **窄侧边栏不再溢出**:移动端压缩布局改用 CSS 容器查询(`container-type: inline-size` + `@container`),跟随**面板自身宽度**而非窗口视口——拖窄 Obsidian 侧边栏即触发,不再有文字溢出。
- 日视图迷你日历可收缩,窄面板自动纵向堆叠(事件在上、日历在下);月视图格子填满窄容器(`minmax(0,1fr)`),7 列完整显示;导航栏自动换行。

## 1.0.1 (2026-08-09)

- **24 小时制时间输入**:事件表单不再跟随系统地区的 12 小时制。时间统一按 `09:00`–`23:59` 输入与显示,中午明确是 `12:00`(绝不再是 `12:00 AM`);`1423`、`900` 这类简写边打边自动格式化,键盘输入更丝滑。
- 表单重构:原生 datetime-local 拆分为日期选择框 + 24 小时制文本时间框;失焦自动补全(`9` → `09:00`),超范围时间(`25:99`)给出提示并禁用保存。

## 1.0.0 (2026-07-29)

首个正式版。

- **同步引擎加固**:认领(Adoption)服务器已知 UID,消除反复重建死循环;本地改动每 10 条增量落盘;写入限速 250ms + HTTP 503 按 2s/5s 退避重试;单条失败不再中断整轮。
- **iCloud 日历兼容**:定时事件创建始终携带 DTEND;全天事件默认 DTEND = 次日,跨月/跨年自动进位;只列出可写的事件日历。
- **移动端可用性**:周/月网格在窄屏下横向滚动(面板头部保持固定);今日在日视图迷你日历中框选;月网格对齐优化。
- **同步体验**:完成后面板自动刷新;同步通知新增认领计数;大批量新建显示进度。

## 0.0.9 (2026-07-28)

- 同步完成后面板自动刷新,不再显示/编辑服务器已替换的旧版本。
- CalDAV 请求 30 秒超时,不再无声挂起。
- iCloud 日历选择只列出真正可写的事件日历(过滤提醒列表)。
- 编辑修复:关闭「全天」不丢已填时间;存储文件夹即时生效;清单视图按天翻页;无法解析的笔记条目给出提示。

## 0.0.8 (2026-07-28)

- iCloud 配置引导:App 专用密码与日历 URL 字段给出获取路径与示例,新增按钮一键获取日历列表。
- 密码字段显隐切换(眼睛按钮)。
- 月视图阅读性:日期格最小/最大宽度,长标题自动换行。

## 0.0.7 (2026-07-22)

- 全天事件更直观:允许开始/结束同一天;跨天事件显示在覆盖的每一天。
- 跨午夜的有时间事件与全天事件正常显示。

## 0.0.6 (2026-07-22)

- 市场自动化修复:release tag 与 manifest 版本严格一致,`minAppVersion` 提升至 1.7.2,为每次 Release 生成工件来源证明。

## 0.0.5 (2026-07-22)

- 移动端响应式布局(480px 以下自动适配)。
- 带颜色的预设分类标签。
- 中英双语界面。
